package com.jaysyko.wrestlechat.auth;

import android.content.Context;

/**
 * CreateNewUser.java
 * Creates and stores a new user into the system
 *
 * @author Jay Syko
 */
public class CreateNewUser {
    /**
     * SignUp the user into the app returns true if successful else returns false
     *
     * @param username String
     * @param password String
     * @return boolean
     */
    public static boolean signUpUser(Context context, final String username, final String password) {
//        new BackEnd(context).execute(stringRequest);
        return true;
    }
}
